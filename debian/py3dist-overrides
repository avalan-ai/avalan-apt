# PyPI distribution name → Debian binary package name overrides for
# dh_python3. Each line maps a `Requires-Dist` name (as it appears in
# the upstream wheel metadata) to the Debian package that provides it.
#
# Refine this list once `dh_python3 --verbose` reports its actual misses
# during the first dry-run build (Slice 4.1 / Slice 4.2).
pillow python3-pil
google-genai python3-google-genai
a2a-sdk python3-a2a-sdk
