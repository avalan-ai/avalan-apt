#!/usr/bin/make -f

export PYBUILD_NAME=rich
export PYBUILD_SYSTEM=pyproject

# Block any accidental network use during build/test.
export PIP_NO_INDEX=1
export PIP_NO_BUILD_ISOLATION=1

%:
	dh $@ --buildsystem=pybuild

# Upstream's tests aren't shipped in the sdist; nothing to run here.
override_dh_auto_test:
	@echo "Skipping tests: rich's sdist does not include the tests/ tree."
