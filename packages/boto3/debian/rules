#!/usr/bin/make -f

export PYBUILD_NAME=boto3

# Block any accidental network use during build/test.
export PIP_NO_INDEX=1
export PIP_NO_BUILD_ISOLATION=1

%:
	dh $@ --buildsystem=pybuild

# Upstream's tests touch live AWS endpoints and pull dev-only
# pytest plugins; skip at build time. The Avalan smoke import
# covers the runtime entry point.
override_dh_auto_test:
	@echo "Skipping upstream tests at build time."
